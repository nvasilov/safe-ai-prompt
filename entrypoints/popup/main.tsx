import React from 'react';
import ReactDOM from 'react-dom/client';
import ExtensionPopup from "@/components/ExtensionPopup.tsx";

ReactDOM.createRoot(document.getElementById('root')!).render(
    <ExtensionPopup/>
)
